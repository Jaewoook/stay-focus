import React from "react";
import styled from "styled-components";
import { Block } from "@salesboost/malta";

export const Layout = styled(Block)`
    padding: 32px 24px;
    background-color: #fff;
`;
